# Historical v2.2 evidence

These files describe v2.1-versus-v2.2 tests and measurements. They are not v2.3 results. v2.3 measures a different, explicitly named compute-pass interval, so do not compare its GPU durations directly to these whole-rendering intervals. Relative paths in historical reports refer to their original release layout.
