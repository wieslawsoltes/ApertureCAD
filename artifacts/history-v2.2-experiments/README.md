# Superseded experiments

These profiles were collected during optimization and include approaches not shipped unchanged. They are not the final release comparison. Use the root-level paired profiles and release-comparison.json. The final source intentionally moved optional text diagnostics outside rendering, then eliminated scans for text-free pages and unchanged samples.
